# Coding session 2
Tasks for today is to implement the use cases of author and viewer

Author can: (with authentication)
- Create Post
- Read Post
- Update Post
- Delete Post
  
Viewer can: (no authentication required)
- Read posts
  

But we are not doing authorization just yet, so let's implement endpoints:
POST /users -> done
POST /posts -> done
GET /posts -> done
GET /posts/{id} -> done
GET /users -> done
GET /users/{id} -> done
